# TODO: Update a_bookings.php Table

- [ ] Update SQL query: Change `AS duration` to `AS lease_length`
- [ ] Update table headers: Remove "Check-Out", change "Duration" to "Lease Length", remove "Commission"
- [ ] Update table rows: Remove check_out_date td, change `$b['duration']` to `$b['lease_length']`, remove commission td
